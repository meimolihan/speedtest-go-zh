# speedtest-go-zh UI 重构计划

## 设计方向：扁平化专业风格（Vercel/Linear 风格）

### 核心设计原则
1. **简洁专业** - 干净的布局，减少视觉噪音
2. **质感细节** - 微妙的阴影、边框、过渡效果
3. **信息层级** - 清晰的视觉层级，突出重要数据
4. **一致体验** - 明暗模式完全适配，支持系统偏好

---

## 技术实现方案

### 1. CSS 变量系统
使用 CSS 自定义属性统一管理所有颜色和间距，便于主题切换和维护。

### 2. 主题系统增强
- 支持 `light` / `dark` / `system` 三种模式
- 使用 `localStorage` 持久化用户选择
- 监听 `prefers-color-scheme` 媒体查询自动切换
- 平滑的主题切换过渡动画

### 3. 设计语言
- **字体**: Inter (正文字体) + JetBrains Mono (数字/代码)
- **圆角**: 统一使用 8px/12px 圆角
- **阴影**: 多层次阴影系统 (sm/md/lg)
- **颜色**: 精简的调色板，主色 + 强调色
- **间距**: 基于 4px 的间距系统

---

## 具体重构内容

### A. HTML 结构优化
1. 添加 `data-theme` 属性到 `<html>` 元素
2. 优化语义化标签
3. 添加 ARIA 属性提升可访问性
4. 重构隐私政策弹窗为模态框组件

### B. CSS 重构
1. **变量系统**
   - 颜色变量（主色、背景、文字、边框）
   - 间距变量（xs/sm/md/lg/xl）
   - 圆角变量
   - 阴影变量
   - 过渡时间变量

2. **基础样式重置**
   - 更现代的 reset
   - 优化的滚动条样式

3. **组件样式**
   - 卡片组件（测试面板）
   - 按钮组件（开始/停止、主题切换）
   - 输入框组件
   - 模态框组件（隐私政策）
   - 徽章/标签组件

4. **布局优化**
   - 改进的网格布局
   - 更好的响应式断点
   - 移动端优化

5. **动画系统**
   - 统一的过渡效果
   - 微交互反馈
   - 加载状态动画

### C. JavaScript 增强
1. 主题管理模块
2. 改进的动画循环
3. 更好的错误处理
4. 键盘导航支持

---

## 颜色方案

### 深色模式
```css
--bg-primary: #0a0a0b;
--bg-secondary: #111113;
--bg-tertiary: #1a1a1d;
--text-primary: #ededed;
--text-secondary: #a1a1a1;
--text-tertiary: #666666;
--border-subtle: rgba(255,255,255,0.08);
--border-default: rgba(255,255,255,0.12);
--accent-blue: #0070f3;
--accent-green: #0ea5e9;
--accent-pink: #ec4899;
--accent-cyan: #06b6d4;
```

### 亮色模式
```css
--bg-primary: #ffffff;
--bg-secondary: #fafafa;
--bg-tertiary: #f5f5f5;
--text-primary: #171717;
--text-secondary: #666666;
--text-tertiary: #999999;
--border-subtle: rgba(0,0,0,0.05);
--border-default: rgba(0,0,0,0.1);
--accent-blue: #0070f3;
--accent-green: #0ea5e9;
--accent-pink: #ec4899;
--accent-cyan: #06b6d4;
```

---

## 组件设计细节

### 1. 顶部导航栏
- 半透明背景 + 毛玻璃效果
- 简洁的标题样式
- 主题切换按钮（太阳/月亮图标）

### 2. 测试卡片
- 干净的白色/深色卡片
- 微妙的边框和阴影
- 圆角设计
- 悬停状态反馈

### 3. 仪表盘
- 简化的弧形设计
- 更清晰的数值显示
- 进度条动画

### 4. 按钮
- 主按钮：实心填充 + 悬停加深
- 次按钮：边框样式
- 平滑的按压反馈

### 5. 模态框
- 居中显示
- 半透明遮罩
- 平滑的进入/退出动画
- 清晰的关闭按钮

---

## 实施步骤

1. **创建新的 CSS 变量系统**
2. **重构 HTML 结构**
3. **重写所有组件样式**
4. **实现主题切换逻辑**
5. **添加动画和过渡效果**
6. **优化响应式设计**
7. **测试明暗模式切换**
8. **验证移动端体验**

---

## 预期效果

- 更专业、更现代的视觉呈现
- 流畅的明暗模式切换
- 一致的用户体验
- 更好的可访问性
- 更易维护的代码结构
